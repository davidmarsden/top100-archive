import React from "react";
import ManagerProfiles from "../ManagerProfiles";

const ManagerProfilesTab = ({ allPositionData, winnersSet }) => (
  <ManagerProfiles allPositionData={allPositionData} winnersSet={winnersSet} />
);

export default ManagerProfilesTab;
